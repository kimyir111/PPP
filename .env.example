# Copy this file to .env and put your real key in it. .env is gitignored.
# Only the local Node service (omr-service.js) reads this — never the browser.
#
#   cp .env.example .env    then edit .env
#
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Defaults to claude-opus-5.
# PPP_COACH_MODEL=claude-opus-5
